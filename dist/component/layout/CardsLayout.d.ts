import React from 'react';
declare type PropsType = {
    children: React.ReactNode;
};
declare function ScreenCenter({ children }: PropsType): JSX.Element;
declare function ResponsifyCards({ children }: PropsType): JSX.Element;
export { ScreenCenter, ResponsifyCards };
