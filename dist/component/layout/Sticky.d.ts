import React from 'react';
declare type PropTypes = {
    leftSide: React.ReactChild;
    rightSide: React.ReactChild;
};
declare function Sticky({ leftSide, rightSide }: PropTypes): JSX.Element;
export { Sticky };
