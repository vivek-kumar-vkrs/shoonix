import React from 'react';
declare type Proptypes = {
    Svg: React.FC;
    title: string;
    msg: string;
};
declare function Success({ Svg, title, msg }: Proptypes): JSX.Element;
export default Success;
